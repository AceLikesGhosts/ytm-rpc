{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "allowJs": true,
        "jsx": "react",
        // This is never used as we cannot build a raw JS file! lol.
        // just here to stop eslint from bitching.
        "outDir": "aaa"
    },
    "include": [
        "./**.*",
        "src/**.jsx"
    ]
}